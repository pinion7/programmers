function solution(a, b) {
  // 분석
  // 수학 법칙 중 가우스 공식을 사용할 수도 있다. 가우스 공식은 아래와 같다.
  // 두 수 사이의 모든 값의 합 = (두 수의 합) * (|두 수의 차| + 1) / 2
  
  // 풀이 3     
  // 1. 두 수의 차만 조심해주면 된다. 절대값 처리해준다.
  return (a + b) * (Math.abs(a - b) + 1) / 2   
}